import React from "react";

const Button = ({ style }) => {
  return (
    <button type="button" className={`py-4 px-6 bg-blue-gradient`}>
      Get Started
    </button>
  );
};

export default Button;
